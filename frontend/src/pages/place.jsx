import React, { useRef } from "react";

const Place = () => {
  const componentRef = useRef(null); // Tạo một ref cho component cần in

  return (
    <div>
      
    </div>
  );
};

export default Place;
